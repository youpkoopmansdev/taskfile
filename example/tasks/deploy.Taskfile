@description Deploy to staging
task staging {
  echo "Deploying $PROJECT to staging in $REGION..."
}

@description Deploy to production
task prod [version] {
  echo "Deploying $PROJECT version $version to production in $REGION..."
}
